# --- MCP Core ---
mcp[cli]>=1.9.0                  # MCP SDK — server (FastMCP) + client + CLI (mcp dev, mcp inspect)

# --- LLM Providers (pick what you need) ---
anthropic>=0.52.0                # Anthropic SDK — Claude models
openai>=1.30.0                   # OpenAI SDK — for projects that use GPT

# --- LangChain + MCP adapter (for agent projects) ---
langchain>=0.3.0
langchain-anthropic>=0.3.0
langchain-mcp-adapters>=0.1.0    # Bridges MCP tools into LangChain agents

# --- UI ---
streamlit>=1.39.0                # Chat UI for agent projects

# --- Utilities ---
python-dotenv>=1.0.0
httpx>=0.28.0                    # Async HTTP client (useful inside MCP tools)
pydantic>=2.0.0
uvicorn>=0.34.0                  # ASGI server — needed for streamable-http transport
